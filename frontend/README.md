# frontend/ (legacy)

This directory contains static HTML templates used during early UI prototyping.
Production UI is the React app in `react-app/`.

Recommended usage:
- Keep for reference or marketing-only experiments.
- Do not deploy this directory in production.
